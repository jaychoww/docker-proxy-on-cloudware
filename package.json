{
  "name": "docker-proxy",
  "version": "1.0.0",
  "main": "src/proxy.js",
  "scripts": {
    "build": "wrangler build",
    "deploy": "wrangler publish"
  },
  "devDependencies": {
    "@cloudflare/wrangler": "^1.21.0"
  }
}
